{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "sage-gateway": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Sage Enforcement Shim (-> Domino AI Gateway)",
      "options": {
        "baseURL": "http://localhost:8080/v1",
        "apiKey": "local-shim-no-auth"
      },
      "models": {
        "deepseek-v4-pro": { "name": "DeepSeek V4 Pro (dev host)", "limit": { "context": 128000, "output": 16384 } },
        "qwen-2-5": { "name": "Qwen 2.5 (sovereign)", "limit": { "context": 32768, "output": 8192 } },
        "local-domino-llm": { "name": "Mistral-7B (sovereign)", "limit": { "context": 32768, "output": 8192 } },
        "bedrock-qwen3-coder": { "name": "Qwen3 Coder 30B", "limit": { "context": 128000, "output": 16384 } },
        "gpt-5.4": { "name": "GPT-5.4", "limit": { "context": 200000, "output": 32768 } },
        "sonnet": { "name": "Claude Sonnet 4.6", "limit": { "context": 200000, "output": 65536 } },
        "haiku": { "name": "Claude Haiku 4.5", "limit": { "context": 200000, "output": 32768 } }
      }
    }
  },
  "model": "sage-gateway/gpt-5.4",
  "permission": {
    "skill": { "*": "allow" }
  },
  "agent": {
    "sage-ask": {
      "mode": "primary",
      "description": "Read-only: answers questions without editing files or running shell commands.",
      "permission": { "edit": "deny", "bash": "deny" }
    },
    "sage-plan": {
      "mode": "primary",
      "description": "Read-only planning: explores and proposes a plan without editing files or running shell commands.",
      "permission": { "edit": "deny", "bash": "deny" }
    },
    "sage-implement": {
      "mode": "primary",
      "description": "Implementation: makes the requested change directly by editing files — it does not merely describe what it would do.",
      "permission": { "edit": "allow", "bash": "allow" },
      "prompt": "You are Sage's implementation agent. The user has already decided WHAT they want — your job is to BUILD it in this turn, not to plan it or describe it.\n\nNon-negotiable:\n- Make the change by editing files with the write/edit tools NOW. Do not answer with a plan, a numbered list of steps you 'would' take, or a description of the change instead of making it. If you find yourself writing 'I will…' or 'Here's what I'll do…', stop and call the edit tool instead.\n- A turn in which you touched no files is a failed turn. If a request is genuinely a pure question, answer it briefly and say it needs no code change — otherwise, edit.\n- Read only what you need to make the edit correctly, then edit. Don't over-explore.\n\nHow to work:\n- Keep changes surgical: touch only what the request requires; match the existing code's style and patterns. Don't refactor, reformat, or 'improve' unrelated code.\n- Follow AGENTS.md in the workspace (data paths, fetch patterns, the don't-copy-data-into-src rule). It overrides your defaults.\n- After editing, verify: the app must build/typecheck. Fix errors you introduced before finishing.\n- Prefer the minimum code that solves the problem. No speculative abstractions or unrequested features.\n\nWhen done, give a one-or-two-sentence summary of what you changed and which files — not a wall of text. The diff is the deliverable, not the prose."
    }
  }
}
